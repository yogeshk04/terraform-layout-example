# staging environment

Root module for the **staging** environment. See
[environments/dev/README.md](../dev/README.md) for the workflow — it's the
same, just with staging-specific `terraform.tfvars`.
